{
  "name": "vertex-studio",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "sanity dev",
    "build": "sanity build",
    "deploy": "sanity deploy",
    "typegen": "sanity schemas extract && sanity typegen generate",
    "seed:import": "sanity dataset import scripts/seed/seed.ndjson --dataset development",
    "seed:import:prod": "sanity dataset import scripts/seed/seed.ndjson --dataset production --replace"
  },
  "dependencies": {
    "@sanity/vision": "^5.31.2",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "sanity": "^5.31.2",
    "styled-components": "^6.5.3"
  },
  "devDependencies": {
    "typescript": "^5"
  }
}
